{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'Using default Nixpacks build'"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "pip install --no-cache-dir torch==2.1.2+cpu torchaudio==2.1.2+cpu -f https://download.pytorch.org/whl/torch_stable.html && pip install --no-cache-dir -r requirements.txt && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}