[phases.setup]
nixPkgs = ["...", "ffmpeg", "python311"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install --no-cache-dir torch==2.1.2+cpu torchaudio==2.1.2+cpu -f https://download.pytorch.org/whl/torch_stable.html",
    "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1"