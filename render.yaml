services:
  - type: web
    name: whisper-api
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1"
    envVars:
      - key: WHISPER_MODEL
        value: base
      - key: PYTHON_VERSION
        value: 3.11.8